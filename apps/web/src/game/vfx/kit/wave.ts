/**
 * Wave kit scaffold — expanding shock fronts.
 * v0 can mount GroundDecal with shape "ring" + a wave preset's colors.
 */
export type { WavePreset } from "./types";
export { wavePresets } from "../presets/ground";
